<?php

//use Filament\Notifications\Notification;

if (!function_exists( 'bhry98_send_filament_notification')) {
    function bhry98_send_filament_notification(string $type, string $message): void
    {
//        match ($type) {
//            'success' => Notification::make()->title($message)->success()->send()->sendToDatabase(auth()->user()),
//            'danger' => Notification::make()->title($message)->danger()->send(),
//            'warning' => Notification::make()->title($message)->warning()->send(),
//            default => Notification::make()->title($message)->info()->send(),
//        };
    }
}

